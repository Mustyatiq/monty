#include "monty.h"

/**
 * nop - does nothing
 * @stack: top of stack
 * @line_number: line number in file
*/

void nop(stack_t **stack, unsigned int line_number)
{
	stack = stack;
	(void)line_number;
}
